<main>
  <!-- <h1>Welcome to <span class="text-gradient">DejaBooth</span></h1> -->
  <header class="hero">
    <p class="instructions">
   
      <img src="public/img/photobooth1_upscaled.jpeg" loading="eager"
      decoding="async" class="parallax bg-img" />
			<img src="public/img/photobooth_frontlayer.png" class="parallax booth-1" />
        </header>
      </main>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 1600px;
		max-width: calc(100% - 4rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
		height: 1vh
	}

.open{
font-size: x-large;

}

	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
    justify-content: center;
		gap: 1rem;
		font-size: xx-large;

		/* max-width: 100%; */
		
	}
	.hero img {
		/* aspect-ratio: 9 / 11; */
		object-fit: cover;
    height: auto;
    width: 90%;
		object-position: center;
    position: relative;
    bottom: 10px;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
    margin-right: 55px;
    margin-bottom: 35px;
	}
	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 6fr 4fr;
			padding-inline: 1.5rem;
			gap: 0px;
		}
	}

	h1 {
		font-size: 3rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
    
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		margin-bottom: 2rem;
    margin-left: 25px;
		/* border: 1px solid rgba(var(--accent-light), 25%); */
		/* background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%)); */
		padding: 2.5rem;
		border-radius: 8px;
		width: 25em;
		length: 15em;
	}

  .capture{
  font-size: 65px;
  }

	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 2rem;
		padding: 0;
	}
</style>